{{ form_start(form) }}
{{ form_row(form.q)}}

<h4 class="mb-1">Categories</h4>
{{ form_row(form.categories)}}

<h4>Prix</h4>
<div class="row">
	<div class="col-md-6">
		{{ form_row(form.min)}}
	</div>
	<div class="col-md-6">
		{{ form_row(form.max)}}
	</div>
</div>
<div id="price-slider">
</div>

<button type="submit" class="btn btn-primary mt-5 mb-5 w-100">Filter</button>


{{ form_end(form) }}
