{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col-md-6 mx-auto text-center">
				<h1 class="my-5">Profil</h1>
				<img src="" alt="">
				<p class="lead">{{ app.user.email }}</p>
				<h3>{{ app.user.fullName }}</h3>
                <p><small>Compte crée le {{ app.user.createdAt|date('d/m/y') }}</small></p>

                <a href="{{ path('app_account_edit') }}">Edit</a> |
                <a href="{{ path('app_account_change_password') }}">Change Password</a>
			</div>
		</div>
	</div>
{% endblock %}
